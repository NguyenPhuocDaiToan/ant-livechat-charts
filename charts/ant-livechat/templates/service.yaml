apiVersion: v1
kind: Service
metadata:
  name: {{ include "ant-livechat.fullname" . }}
  labels:
    {{- include "ant-livechat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ant-livechat.selectorLabels" . | nindent 4 }}